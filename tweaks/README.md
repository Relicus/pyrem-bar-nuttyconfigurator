# Tweaks Notes

- `Units_*.lua` files and a few special tweak files in this folder are configurator fragments, not standalone Lua chunks.
- `bar-configurator/tweaks/.luarc.json` ignores those fragment files so folder-wide LuaLS checks stay useful for real script files like `Defs_Waves_Doom_Mode.lua`.
